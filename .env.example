# =============================================================================
# Velociraptor Forensic MCP Server — Configuration
# =============================================================================
# Copy this file to .env and edit the values.
# Either VELOCIRAPTOR_API_KEY or SAFE_BASE (or both) must be set.

# --- Velociraptor Remote Connection ---
VELOCIRAPTOR_API_KEY=./api.config.yaml
VELOCIRAPTOR_SSL_VERIFY=false
VELOCIRAPTOR_TIMEOUT=30

# --- Local Forensic Toolkit ---
SAFE_BASE=/home/youruser/evidence

# --- MCP Server ---
MCP_SERVER_HOST=127.0.0.1
MCP_SERVER_PORT=8100
LOG_LEVEL=INFO

# --- Feature Toggles ---
# Comma-separated tool names to disable:
# DISABLED_TOOLS=vr_collect_artifact,local_scan_syslog
DISABLED_TOOLS=

# Block write operations (artifact collection):
READ_ONLY=false
